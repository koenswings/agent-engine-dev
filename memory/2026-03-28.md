# Memory — 2026-03-28

## Session Context (pre-compaction flush, 11:23 UTC)

### Active Work: Automated Test Setup (Proposal B — Native Engine)

**Status:** PR #9 (test/pr1-disk-simulation) is open, awaiting Koen's review/merge.

**Design docs (all merged to main via PR #4 and PR #8):**
- `design/test-setup-virtual.md` — Proposal A (Docker Battery), status: Superseded
- `design/test-setup-native.md` — Proposal B (Native Engine), status: Approved
- `design/test-setup-comparison.md` — 9-dimension comparison, recommends B, status: Approved

**PR #9 contents (branch: test/pr1-disk-simulation):**
- `src/data/Config.ts`: `testMode: boolean` in Settings; `IDEA_TEST_MODE=true` env var override
- `config.yaml`: `testMode: false` added
- `src/data/Meta.ts`: returns `devMeta` in testMode (prevents `process.exit(1)` on dev machines)
- `src/monitors/usbDeviceMonitor.ts`: mount/umount/mkdir/rm skipped in testMode; initial device enumeration skipped
- `test/harness/diskSim.ts`: `createTestStore()`, `dockFixture()`, `triggerUndock()`, `cleanupDisk()`, `waitFor()`
- `test/fixtures/disk-kolibri-v1/META.yaml` + `apps/kolibri-1.0/compose.yaml`
- `test/automated/disk-dock-undock.test.ts`: 3 tests (dock registers, undock removes, re-dock re-registers)
- `package.json`: `test:unit` script added

**Remaining PRs (2–6) not started:**
- PR 2: instance lifecycle tests (`waitForStatus()` helper, `test/automated/instance-lifecycle.test.ts`)
- PR 3: app versioning + upgrade tests (`isMinorUpdate()` added to Engine)
- PR 4: multi-engine network tests (mDNS, CRDT sync, `assignAppsToEngines()`)
- PR 5: diagnostic mode (`pnpm test:diagnostic`, `test/diagnostic/field-health.test.ts`)
- PR 6: Mocha → Vitest migration

### Key Technical Decisions
- `testMode` skips only `sudo mount`/`sudo umount`/mkdir/rm in usbDeviceMonitor; all engine logic runs
- `IDEA_TEST_MODE=true` env var overrides config to avoid module-init race (Engine.ts has top-level await)
- `Meta.ts` returns `devMeta` when `isDev || testMode` — prevents process.exit on dev/CI machines
- Multi-engine app assignment: sorted round-robin `engines[i % N]` by alphabetical appId — deterministic, zero config
- Diagnostic mode: discovers real mounted disks, replays dock/undock via sentinel file, smoke tests, no internet
- Test device name: `sdtest1`; test paths: `/disks/sdtest1/`, `/dev/engine/sdtest1`

### App Fixture Structure
```
test/fixtures/disk-kolibri-v1/
  META.yaml  (diskId: "test-fixture-kolibri-v1", isHardwareId: false)
  apps/kolibri-1.0/
    compose.yaml  (x-app.title=Kolibri, image=learningequality/kolibri:v0.16.3)
```

### Test Scripts
- `pnpm test:unit` — `IDEA_TEST_MODE=true mocha 'dist/test/automated/*.test.js'`
- `pnpm test` / `pnpm test:full` — full suite including automated tests

### Open Design Questions
1. Version comparison — semver strict vs major-only?
2. SSH key distribution for remote Pi control
3. Image pre-pull strategy
4. testMode default ON in Pi production builds?
5. Diagnostic report format — terminal only or write to disk META.yaml?

### Cross-Agent Updates
- All 6 agents' `AGENTS.md` updated (startup checklist, Documentation Rules, same-PR rule, memory/updates protocol)
- Veri's review checklist extended: authoritative doc verification + Design PRs section
- `TOOLS.md` merge conflict in engine-dev main identified (delegated to Compass)

### Repo State
- Engine repo: `/home/node/workspace/agents/agent-engine-dev/`
- Current branch: `test/pr1-disk-simulation`
- Open PRs on engine repo: #9 (PR 1 of 6 — disk simulation)
- Next: wait for Koen to merge PR #9, then implement PR 2 (instance lifecycle)

## Decisions Added 2026-03-28 ~13:30 UTC

### Open question resolutions
- **Q1 (version comparison):** Major-number comparison is correct. All current/planned app versions use integer-major style (1.x, 2.x). No 0.x ambiguity. `1.0 → 1.1` = minor/compatible; `1.x → 2.x` = blocked as incompatible major upgrade.
- **Q3 (image pre-pull):** Auto-pull if missing — engine runs `docker pull` inline before starting a container if image not present. No formal pre-staging for now; revisit when field deployment picture is clearer.
- **Q2 (SSH keys):** Task added to MC board (`904feb39`). Design note added as comment: lean toward per-engine keypairs + LAN key exchange (option B) over shared keypair (option A).

### New backlog tasks added
- `34c37e24` — Fix extractAppVersion for hyphenated app names (bug, engine)
- `904feb39` — Plan and implement SSH key-based access for field Pis (security, infrastructure, engine)

## Decisions Added 2026-03-28 ~14:00 UTC (session continuation)

### Remaining open question resolutions
- **Q4 (testMode on Pi):** Yes — `testMode: true` is the default in production Pi builds. Enables `pnpm test:diagnostic` without any config change. Only effect in production: skips `sudo mount/umount` (irrelevant since disks are already mounted in field).
- **Q5 (diagnostic report format):** Write to `test/testresults/diagnostic-report-YYYY-MM-DD-HHMM.log` in the engine repo root. `test/testresults/` is gitignored entirely. Terminal output also shown. No writes to META.yaml.

### Test execution environment
- **No cloud runners.** All tests (automated + diagnostic) run on Koen's local Pi setup only.
- **1–3 engines.** `pnpm test:diagnostic` auto-discovers engines via mDNS; round-robin assignment adapts to N=1, 2, or 3 with no config changes.

### App compatibility testing (new decision)
- App-specific tests + test-data snapshots live in each app repo (`app-kolibri/tests/`, `app-kolibri/test-data/`), not in the engine repo.
- A new App Maintainer agent will own the shared test harness and run upgrade tests across all app repos. Atlas asked to propose this agent (message sent 2026-03-28 ~13:45 UTC to Telegram group -5105695997).
- Engine repo fixtures contain only YAML (META.yaml + compose.yaml) — no binary data, no Docker images.

### All 5 design questions now closed
| # | Question | Decision |
|---|----------|----------|
| 1 | Version comparison | Major-number only (1.x vs 2.x). All current apps use integer-major. |
| 2 | SSH key distribution | Per-engine keypairs + LAN key exchange. Backlog task 904feb39. |
| 3 | Image pre-pull strategy | Auto-pull if missing (docker pull inline). |
| 4 | testMode default on Pi | Yes — default true in production Pi builds. |
| 5 | Diagnostic report format | `test/testresults/diagnostic-report-YYYY-MM-DD-HHMM.log`, gitignored. |
