# Memory — 2026-03-30

## Session Summary
Pre-compaction flush. Session covered PRs 1–3 of 6 (all merged), PR 4 planning, and SSH key setup.

## Current State
- **Active task**: PR 4 of 6 — Multi-engine network tests (branch not yet created)
- **All PRs merged**: #9 (disk sim), #16 (instance lifecycle), #20 (app versioning), #19 (security fixes)
- **design/test-setup-native.md**: status = "Partially Implemented — PR 3 of 6 complete"

## PR 4 Plan
- Branch: `test/pr4-multi-engine` from `main`
- Must implement `assignAppsToEngines()` as pure function in `Store.ts` first (does NOT exist yet)
- Tier 1: always-run unit tests — engine registration, `getRunningEngines()`, `getLocalEngine()`, `createOrUpdateEngine()`
- Tier 2: auto-skip if no LAN peers discovered via mDNS
- Store assertions table already written in `design/test-setup-native.md`

## SSH Key Status
- Ed25519 key at `/home/node/workspace/.ssh/id_ed25519`
- `command=` restriction LIVE on Pi — SSH triggers `run-tests.sh`
- `TEST_SSH_KEY` in `.env`
- **Blocker**: root-owned `dist/` on Pi — Koen must run `sudo chown -R pi:pi /home/pi/idea/agents/agent-engine-dev/dist/`
- Once fixed: SSH test execution should work end-to-end

## Key Decisions Made This Session
- Major upgrade blocking REMOVED from PR #20 (wrong premise — data lives on disk, v2.0 disk IS a new instance)
- Cross-disk upgrade detection (future): `isMajorUpgrade()` + `extractMajorVersion()` exist in `App.ts` as utilities
- Correct upgrade scenario: two disks docked simultaneously; engine PROPOSES upgrade; Console Admin initiates rsync
- `waitForHttpDown` polls until connection refused (more reliable than one-shot timeout)
- Race condition fix: disk-dock-undock `after()` must remove disk FIRST, wait 2s, then clean containers
- Tables → PNG images via `render_table.py` skill; never send raw markdown tables to Telegram

## Fixture Structure
```
test/fixtures/
  disk-sample-v1/       (traefik/whoami, version 1.0)
  disk-sample-v1.1/     (traefik/whoami, version 1.1)
  disk-sample-v2/       (traefik/whoami, version 2.0)
  disk-kolibri-v1/      (kolibri app, version 1.0)
```

## Key Source Files
- `src/data/Store.ts` — `engineDB`, helpers: `getRunningEngines()`, `getLocalEngine()`, `createOrUpdateEngine()`
- `src/data/Network.ts` — `connectEngine()`, `disconnectEngine()`, `manageDiscoveredPeers()`
- `src/monitors/mdnsMonitor.ts` — advertising via `@homebridge/ciao`; discovery via `node-dns-sd`
- `test/harness/diskSim.ts` — `createTestStore()`, `dockFixture()`, `waitFor()`, `waitForHttpDown`, `cleanupContainers`

## Backlog Tasks (active)
- `34c37e24` — Fix extractAppVersion for hyphenated app names
- `904feb39` — SSH key plan for field Pis
- `83f843a4` — Find logical inconsistencies between architecture, solution description and current code
- `432c90cc` — Upgrade zx v7→v8 (CVE-2025-13437)
- `d5ad1fcf` — Remove App concept
- `e3e8e31d` — Cross-disk upgrade detection
- `c74d6a8d` — Scan Solution Description for unimplemented features
- `50167212` — Review run architecture: user, file ownership, permissions
- `566a0820` — Refactor script/ to scripts/
- `7ea92164` — Remove Docker dev environment support
- `c9cb8515` — Update Architecture doc from Solution Description
- `3b0f08f6` — Review and improve Solution Description
- `2fc2631b` — Test permanently attached USB SSD as system disk

## Infrastructure
- Pi host: `172.20.0.1`; hostname `wizardly-hugle`; engine repo at `/home/pi/idea/agents/agent-engine-dev/`
- Engine WebSocket port: `4321`; engine NOT currently running on Pi
- MC API: agent token only writes own board (6bddb9d2); cross-board = 403; use Telegram for cross-agent
- Atlas Telegram: `-5105695997`; Axle Telegram: `-5146184666`
- Table rendering: `/home/node/workspace/skills/telegram-table/scripts/render_table.py`
