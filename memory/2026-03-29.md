# Memory — 2026-03-29

## Session: SSH key restriction + PR 2 lifecycle tests

### Status at session end (compaction flush)

**Current branch:** `test/pr2-instance-lifecycle` (PR #16)
- 5/5 instance-lifecycle tests passing (real `traefik/whoami` containers, HTTP health checks)
- PR open, awaiting Koen's review and merge

**SSH key restriction task `ebfa743b` — IN PROGRESS (not yet complete):**
- Ed25519 keypair at `/home/node/workspace/.ssh/id_ed25519`
- Public key currently installed **unrestricted** on `pi@172.20.0.1`
- `scripts/run-tests.sh` exists on Pi at `/home/pi/idea/scripts/run-tests.sh`
  - Root-owned; executable
  - Path bug fixed: `ENGINE_DIR=/home/pi/idea/agents/agent-engine-dev` (was missing `agents/`)
  - Wraps: `git pull && pnpm build && IDEA_TEST_MODE=true pnpm test:unit`
- **Restricted `authorized_keys` line constructed but NOT yet applied:**
  ```
  command="/home/pi/idea/scripts/run-tests.sh",from="172.20.0.0/24",restrict ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIO3EvH5TihFSAA9q+VFqnswFOEtLtbZVwgtOVXpq82I4 openclaw-container->pi-host-tests [2026-03-28]
  ```
- **Next steps for task `ebfa743b`:**
  1. Apply restricted line to Pi's `/home/pi/.ssh/authorized_keys` (replace plain `openclaw-axle@idea` entry)
  2. Add `TEST_SSH_KEY=/home/node/workspace/.ssh/id_ed25519` to `.env`
  3. Run end-to-end SSH verification (container → Pi → `run-tests.sh` → test results)
  4. Mark task `ebfa743b` done on Atlas's board

### Open PRs

**Engine repo:**
- PR #14 — `chore/remove-tmux-vscode` (open)
- PR #15 — `memory/updates` (open, awaiting merge)
- PR #16 — `test/pr2-instance-lifecycle` (open, 5/5 passing, ready for review)

**Idea repo (Atlas):**
- PR #11 — SSH key management design (reviewed by Axle, comments posted)
- PR #13 — SSH key implementation with `scripts/run-tests.sh`; path bug fixed by Axle via `sudo sed -i`

### Backlog tasks (MC)
- `34c37e24` — Fix `extractAppVersion` for hyphenated app names (`appId.split('-')[1]` breaks)
- `904feb39` — SSH key plan for field Pis (per-engine keypairs + LAN key exchange)

### Next PRs planned (after PR #16 merges)
- PR 3: App versioning + upgrade tests (`isMinorUpdate()`, `disk-sample-v2/` fixture)
- PR 4: Multi-engine network tests (mDNS, CRDT sync, `assignAppsToEngines()`)
- PR 5: Diagnostic mode (`pnpm test:diagnostic`, `test/diagnostic/field-health.test.ts`)
- PR 6: Mocha → Vitest migration

### Key decisions (durable)
- `testMode: true` in prod config: skips only `sudo mount/umount`; all other engine logic runs normally
- `IDEA_TEST_MODE` env var overrides `testMode` at process start (avoids module-init race)
- `enableUsbDeviceMonitor` returns watcher; disk tests close it in `after()` (prevents double-fire in lifecycle tests)
- No `testMode` simulation guards in `Instance.ts` — real containers required
- `TEST_HOST=172.20.0.1` in sandbox container (ports bind on Pi host, not container loopback)
- Test device: `sdz1`; pattern `sd[a-z][1-2]`
- Test results written to `test/testresults/` (gitignored)
- Atlas task: cross-board task creation returns 403 (agent token only writes own board); use Telegram for cross-agent messages
- App compatibility tests live in each app repo (not engine repo); new App Maintainer agent owns shared harness

### Infrastructure
- Pi host: `172.20.0.1` (Docker bridge gateway); hostname `wizardry-hugle`
- Engine repo on Pi: `/home/pi/idea/agents/agent-engine-dev/`
- SSH key private: `/home/node/workspace/.ssh/id_ed25519`
- Atlas Telegram group: `-5105695997`
- Axle Telegram group: `-5146184666`

## Communication preferences (added 2026-03-30)
- **Tables → images**: Always render tables as PNG using `/home/node/workspace/skills/telegram-table/scripts/render_table.py` and send via `message` tool with `media=`. Never send raw markdown tables in Telegram replies.
